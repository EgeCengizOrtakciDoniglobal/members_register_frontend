# Backend API taban adresi (Laravel). Sonunda /api olmalı.
VITE_API_URL=http://localhost:8000/api

# WordPress entegrasyon ucu için secret key.
# Backend .env'deki INTEGRATION_API_KEY ile AYNI olmalı.
VITE_INTEGRATION_API_KEY=
